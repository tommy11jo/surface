version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: ["npm", "start"]
    depends_on:
      - redis-persistent
      - redis-cache

  redis-persistent:
    image: redis/redis-stack:latest
    ports:
      - "6380:6379"
    environment:
      - REDIS_ARGS=--appendonly yes --maxmemory 1gb
    volumes:
      - ./local-data:/data

  redis-cache:
    image: redis/redis-stack:latest
    ports:
      - "6379:6379"
    environment:
      - REDIS_ARGS=--maxmemory 1gb
